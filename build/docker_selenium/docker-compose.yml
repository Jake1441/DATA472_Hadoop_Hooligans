services:
  python_scraper:
    image: desertroam/hadoophulligans:latest
    volumes:
      - scraper-db:/app/src
      - $HOME/.env:/app/src/.env
    container_name: python-scraper

volumes:
  scraper-db: