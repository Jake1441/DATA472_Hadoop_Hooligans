---
- name: admin packages
  hosts: localhost
  roles:
    - distupgrade

- name: Install docker only
  hosts: localhost
  become: true
  roles:
    - docker-role

- name: Install log server only
  hosts: localhost
  become: true
  roles:
    - logstash
# placed here incase docker has not started yet
- name: add uesr to docker role
  hosts: localhost
  roles:
    - docker-group