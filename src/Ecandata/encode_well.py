# import base64
print("This script is no longer in use")
assert False

# def encode_well(well_code:str) -> str:
#     def encode_string(string):
#         return base64.b64encode(string.encode()).decode()
#     encoded_well_number = encode_string(well_code)
#     base_url = "https://www.ecan.govt.nz/data/well-search/welldetails/"
#     href = f"{base_url}{encoded_well_number}/{encoded_well_number}"

#     return href


# #codes_to_test = ['M35/6040', 'M35/7281', 'M35/1382', 'M35/4875']
# #for code in codes_to_test:
# #    print(encode_well(code))